{
  "name": "@portfolio/database",
  "version": "0.1.0",
  "description": "Prisma schema and generated client for the portfolio platform.",
  "private": true,
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
